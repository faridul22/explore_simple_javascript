const numbers = [2, 4, 6, 8, 10, 12]

// to get array index
console.log(numbers.length)

// to get an index element by element
console.log(numbers.indexOf(4))

// to get an index element
const thirdElement = numbers[2];
console.log(thirdElement)